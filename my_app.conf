{
    # This replaces the secret you were pulling from the database.
    # You can generate a new one with: perl -Mojo::Util -E 'say secret_token'
    secrets => ['YourSuperSecretStringHere'],
    
    # You can also move your Hypnotoad settings here
    hypnotoad => {
        listen => ['http://0.0.0.0:8082'],
        proxy  => 1,
    }
}
